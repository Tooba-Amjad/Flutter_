name: untitled1
description: "A new Flutter project."

# Prevents accidental publishing to pub.dev.
publish_to: 'none'

# Versioning
version: 1.0.0+1

# Flutter and Dart SDK environment setup
environment:
  sdk: ">=3.5.0 <4.0.0"

# Project dependencies
dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.1.0
  flutter_colorpicker: ^1.0.3
  cupertino_icons: ^1.0.8

# Development dependencies
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

# Flutter-specific settings
flutter:
  uses-material-design: true

  # Assets
  assets:
    - assets/note1.wav
    - assets/note2.wav
    - assets/note3.wav
    - assets/note4.wav
    - assets/note5.wav
    - assets/note6.wav
    - assets/note7.wav
